name: Test Develop Trigger

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Workflow triggered on develop branch!"
        